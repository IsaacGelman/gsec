" Enable line numbers and relative number
set number
set relativenumber

" Enable mouse support
 set mouse+=a

" Syntax highlighting
syntax on

" Case insensitive search
set ignorecase
set smartcase

" Ignore keybind Q in normal mode
nmap Q <Nop>

" Disable error bells
 set noerrorbells visualbell t_vb=
